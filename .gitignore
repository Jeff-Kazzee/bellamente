node_modules/
dist/
coverage/
bella
bella.exe
bella-probe-*
*.log
.env
.env.local
pgdata/
*.tmp
.DS_Store
.vercel/
.release/

# Internal/semi-proprietary design docs + reverse-engineering provenance.
# Kept in the working folder, NOT published to GitHub.
/docs/

# Brand brief + source assets live in docs/ (local-only, for the website/app work);
# never commit copies dropped at the repo root. Anchored to the ROOT on purpose: a future
# tracked website/src "brand/" assets directory must not be silently ignored.
/BRAND.md
/brand/
