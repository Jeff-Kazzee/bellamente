interface:
  display_name: "Bellamente"
  short_description: "Repo-specific patterns and workflows for bellamente"
  default_prompt: "Use the bellamente repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true